{
  "name": "lambda-cljs",
  "version": "1.0.0",
  "description": "cljs lambda",
  "main": "main.js",
  "scripts": {
    "build": "shadow-cljs release :aws --debug",
    "predeploy": "npm run build",
    "deploy": "cd out/ && zip lambda.js",
    "dev": "shadow-cljs watch :dev",
    "test": "shadow-cljs watch :test",
    "server": "shadow-cljs server"
  },
  "dependencies": {
    "aws-sdk": "^2.813.0"
  },
  "devDependencies": {
    "shadow-cljs": "^2.11.8",
    "websocket": "^1.0.33"
  }
}
